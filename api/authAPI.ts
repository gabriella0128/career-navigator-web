const apiBase = "/api/auth";

export const AuthAPI = {
  USER_LOGIN: `${apiBase}/login`,
  REISSUE_TOKEN: `${apiBase}/reissue`,
  USER_LOGOUT: `${apiBase}/logout`,
  PAGE_AUTH_CHECK: `${apiBase}/page-auth`,
};
